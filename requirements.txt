# NIC MedSearch — Python dependencies
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
tqdm>=4.65.0

# Text cleaning
ftfy>=6.1.1
pyspellchecker>=0.7.2

# PDF processing
pymupdf>=1.23.0          # fitz — digital PDF text extraction
pdf2image>=1.16.3        # scanned PDF → image conversion
pytesseract>=0.3.10      # OCR engine wrapper
pdfplumber>=0.10.3       # PDF table extraction

# DOCX processing
python-docx>=1.1.0

# RAG pipeline (next phase)
sentence-transformers>=2.7.0   # local embeddings (all-MiniLM-L6-v2)
qdrant-client>=1.9.0           # vector database client
ollama>=0.2.0                  # local LLM wrapper

# UI
streamlit>=1.35.0
